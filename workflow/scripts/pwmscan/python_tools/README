PWM scan utilities
------------------

- matrix_scan_parallel.py  Run the matrix_scan program in parallel by distributing the scanning task on several processing units. 
                           It requires a multi-core processor.

NOTE:
The program matrix_scan_parallel.py relies on the python parallelize.py script that can be found in the python directory.
